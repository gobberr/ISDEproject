extends layout

block content

  .py-5
    .container
      .row
      h5 List of UniTN rooms
      .row
      br 
      #aule
      if (aule.length>0)      
        each aula in aule
          h7 #{JSON.stringify(aula)}
      else
        h7 Nessun aula libera trovata
      
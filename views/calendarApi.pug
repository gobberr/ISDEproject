extends layout

block content

  .py-5
    .container
      .row
      h5 List of calendar events
      .row
      br
      #events        
      if (events.length>0)
        each event in events    
          h7 #{JSON.stringify(event)}       
      else
        h7 Nessun evento trovato nel calendario
          
          